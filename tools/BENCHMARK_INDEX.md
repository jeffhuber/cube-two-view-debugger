# Benchmark index

> Which fixture / report / script answers which question.
> Treat this as a one-page lookup — if you can't find the question
> here, the answer probably doesn't exist yet (good signal).

## Fixtures (labeled data)

| Path | What it is | n | Format | Used by |
|---|---|---|---|---|
| `tools/FULL_CORNER_LABELING.md` | **Canonical corner convention** for `Va/Vb + 0..5`, including A/B face outlines and flattened-net facelet mapping | n/a | documentation + shared constants in `tools/corner_conventions.py` | all new full-corner labels and migrations |
| `tests/fixtures/full_corner_ground_truth.json` | **Canonical full-corner truth.** Human-labeled `Va/Vb + 0..5` corners for convention validation, oracle rectification, and targeted tail diagnostics. | 34 photos (17 sets × A/B) | per-key: vertex, corner_0..corner_5, approved, optional yaw_quarter_turns | source of truth for validating A/B corner semantics before migrating legacy fixtures |
| `tests/fixtures/full_corner_global_model_baseline.json` | **Canonical global-model seed baseline.** Current global model scored against the original full-corner seed truth, not legacy `near_*`; not yet regenerated for tail labels such as Set 70. | 12 photos (6 sets × A/B) | summary + per-row one-edge/far triplet metrics | `tools/baseline_full_corner_global_model.py` + `FULL_CORNER_GLOBAL_MODEL_BASELINE.md` |
| `tests/fixtures/gcm_axis_ground_truth.json` | **Legacy vertex + `near_*` fixture.** Treat as provisional until audited/migrated against full-corner truth; today we do not assume `near_x/near_y/near_z` are the one-edge triplet. | 58 photos (29 sets × A/B) | per-key: vertex, near_x, near_y, near_z, approved | legacy `tools/baseline_post_218.py`; migration input only |
| `tests/fixtures/gcm_vertex_ground_truth.json` | **Vertex only** per photo (earlier, smaller corpus) | 27 photos | per-key: vertex | older vertex evaluators (superseded by axis ground truth) |
| `tests/fixtures/post_218_baseline.json` | **Legacy accuracy snapshot** of current-main global model on the axis-labeled set, run 2× per case = 116 runs. Provisional until regenerated from full-corner truth. | 116 runs / 58 cases | per-case list of run results | legacy regression gate via `--diff` |
| `tests/fixtures/cv_local_baseline.json` | **Legacy cv-local geometry snapshot** on the same axis-labeled cases, with (vertex, 3 near, 3 far) derived from face-quad union-find clustering. Provisional until regenerated from full-corner truth. | 58 runs / 58 cases | per-case list of run results | legacy cross-side comparison; Phase 2 trust-signal input |
| `tests/fixtures/main_solvable_baseline.json` | **Committed solvable-rate snapshot** of current-main production recognizer on the corpus + hard-case manifests. Tracks per-sticker accuracy, exact matches, legal states, confident solves, and confident-wrong rows. | 42 rows (41 scored, 1 skipped) | aggregate + per-row metrics | `tools/main_solvable_baseline.py`; Phase 2/3 production guardrail baseline |
| `tests/fixtures/phase2b_trust_signal_matrix.json` | **Phase 2B trust-signal matrix (join-only).** Per-run join of phase_sep (from `post_218_baseline.json`) + cv-local structural status (from `cv_local_baseline.json`) + outcome category, plus candidate-rule evaluation against the Phase 2 bar (≥80% catastrophic recall at ≤10% GOOD false-retake). **Headline: no rule over existing signals meets the bar.** | 116 rows / 58 cases / 18 rules | per-run signals + per-rule eval | `tools/phase2b_trust_matrix.py` + `PHASE_2B_TRUST_SIGNAL_MATRIX.md` |
| `tests/fixtures/phase2b_recomputed_signals.json` | **Recomputed global-model signals.** Per-run capture of `fit_residual_rms_px`, `pnp_rms_px`, `hexagon_centroid_vs_bezel_vertex_offset_px`, `junction_score_at_ensemble`, `ensemble_shift_px`, and phase_sep at native precision, regenerated by re-running the current-main global model on the 58-case axis-labeled gallery (2 runs per case for non-determinism). | 116 runs / 58 cases | per-case list of run records | `tools/phase2b_recompute.py` (produces) + `tools/phase2b_trust_matrix.py --recompute-global-model` (consumes) |
| `tests/fixtures/phase2b_trust_signal_matrix_recomputed.json` | **Phase 2B trust-signal matrix (with recomputed signals).** Joins the recomputed signals with cv-local status; evaluates 54 candidate rules from both `candidate_rules()` and `recomputed_signal_rules()` (sweeps on fit_residual, pnp_rms, hex↔bezel disagreement, ensemble_shift, junction_score, plus OR/AND compounds with the strongest existing rule). **Headline: still no rule meets the bar, but `phaseANDcv_OR_ensemble_shift_T60.0` is first compound to clear the FPR bar at 8.1% / 50% recall. Triggers Codex's conditional pivot to learned-geometry / capture-UX.** | 116 rows / 58 cases / 61 rules | per-run signals + per-rule eval | `tools/phase2b_trust_matrix.py --recompute-global-model` + `PHASE_2B_TRUST_SIGNAL_MATRIX_RECOMPUTED.md` |
| `tests/fixtures/overlay_visual_labels.json` | **Hex-fitter overlay correctness** labels from May-18 review | (varies) | per-case overlay verdict | `tools/generate_hex_fitter_walkthroughs.py` |
| `tests/fixtures/vertex_axis_active_learning_feedback_v0.json` | Active-learning iteration of vertex/axis labels | (varies) | per-case feedback | `tools/active_vertex_axis_label_queue.py` |
| `tests/fixtures/trihedral_junction_extraction_v0_summary.json` | Per-case extracted trihedral junction quality summary | (varies) | per-case summary | `tools/trihedral_junction_extraction_v0.py` |
| `tests/fixtures/axis_ray_vertex_refinement_v0_summary.json` | Per-case axis-ray refinement output | (varies) | per-case summary | `tools/axis_ray_vertex_refinement_v0.py` |

## Scripts that produce baselines

| Script | What it benchmarks | Output | Notes |
|---|---|---|---|
| **`tools/baseline_post_218.py`** | **Legacy global model accuracy vs axis-labeled ground truth.** Categorizes each case GOOD / MARGINAL / CHIRALITY_MISS / CHIRALITY_FALSE_FLIP / TRUE_GEOMETRY_FAIL. | `tests/fixtures/post_218_baseline.json` + `POST_218_BASELINE_AND_TAXONOMY.md` | Provisional because the `near_*` targets are legacy far/double-axis labels on the seed audit. Use `--diff` for legacy row-level comparisons only until migrated. |
| **`tools/baseline_full_corner_global_model.py`** | **Canonical global model accuracy vs full-corner truth.** Categorizes one-edge/far triplets as GOOD / MARGINAL / PHASE_SWAPPED / GEOMETRY_FAIL. | `tests/fixtures/full_corner_global_model_baseline.json` + `FULL_CORNER_GLOBAL_MODEL_BASELINE.md` | First canonical phase/parity snapshot. Seed result: 8/12 `PHASE_SWAPPED`, 3/12 `GOOD`, 1/12 `MARGINAL`; use this before mining phase diagnostics. |
| **`tools/baseline_cv_local.py`** | **Legacy cv-local accuracy vs axis-labeled ground truth** (same cases). Derives (vertex, 3 near, 3 far) from cv-local's 3 face-quads via union-find clustering of 12 corner instances. Same categorization as baseline_post_218. | `tests/fixtures/cv_local_baseline.json` + `PHASE_1_CV_LOCAL_BASELINE.md` | Companion to baseline_post_218 for historical cross-side comparison. Regenerate from full-corner truth before treating phase categories as canonical. |
| **`tools/main_solvable_baseline.py`** | **Current-main production solvable-rate baseline** from `tools/probe_corpus.py` JSON outputs over corpus + hard-case manifests. | `tests/fixtures/main_solvable_baseline.json` + `MAIN_SOLVABLE_BASELINE.md` | Direct successor to the older #139 per-sticker solvable-rate number; also records exact/legal/confident/confident-wrong rates. |
| **`tools/diagnose_slot_yaw_assignment.py`** | **Hull-label slot/yaw assignment diagnostic.** Maps hull-label slots to WCA faces using assumed, manifest, hull-label center-color, and legacy-detected yaw sources, then scores convention-oriented stickers against ground truth. | `tests/fixtures/hull_label_slot_yaw_assignment.json` + `tools/HULL_LABEL_SLOT_YAW_ASSIGNMENT.md` | Current result: hull-label center-color yaw assembles 40/41 rows and reaches 19 exact, outperforming legacy detected yaw coverage (27 assembled, 14 exact). Use this to decide production yaw-source priority. |
| **`tools/validate_hull_label_tier1_recognizer.py`** | **Production-shaped Hull-Label Tier 1 recognizer validation.** Runs `WhiteUpRecognizer` in `off`, `shadow`, raw hull-label candidate, and effective-prefer modes on the corpus, capturing real face identity, legal-state, color repair, fallback, yaw, and capture-guidance diagnostics. | `tests/fixtures/hull_label_tier1_recognizer_validation.json` + `tools/HULL_LABEL_TIER1_RECOGNIZER_VALIDATION.md` | Post-#305 answer for whether hidden/default-off prefer mode is ready to promote. Current result: shadow matches legacy, effective prefer selects 4/41 rows, improves set 39, no regressions; bottleneck is face counts/centers after rectification, not hull geometry. |
| **`tools/phase2b_trust_matrix.py`** | **Phase 2B trust-signal matrix.** Two modes: default joins existing per-run (phase_sep) + per-case (cv-local status) signals into a per-run matrix, evaluates 18 candidate trust rules. `--recompute-global-model` extends with 6 continuous signals (fit_residual, pnp_rms, hex↔bezel disagreement, ensemble_shift, junction_score, phase_sep native precision) via `tools/phase2b_recompute.py`, evaluates 61 rules. Diagnostics-only — no production behavior change. | `tests/fixtures/phase2b_trust_signal_matrix.json` + `PHASE_2B_TRUST_SIGNAL_MATRIX.md`, and `..._recomputed.json` + `..._RECOMPUTED.md` | **Verdict:** no hand-tuned rule clears both bars (≥80% recall AND ≤10% GOOD FPR) on either matrix. With recomputed signals, `phaseANDcv_OR_ensemble_shift_T60.0` is first rule to clear the FPR bar (8.1%) but at 50% recall. Triggers Codex's conditional pivot to Phase 4 (learned ranker on the 6 continuous signals) or Phase 5 (capture-UX leveraging diagnostics). |
| **`tools/phase2b_recompute.py`** | **Re-run the global model per case** to extract continuous trust signals (fit_residual_rms_px, pnp_rms_px, hexagon_centroid_vs_bezel_vertex_offset_px, junction_score_at_ensemble, ensemble_shift_px, phase_darkness_separation) from `model.debug`. Output is the per-run fixture consumed by `tools/phase2b_trust_matrix.py --recompute-global-model`. | `tests/fixtures/phase2b_recomputed_signals.json` | ~14 min for full 58-case x 2-run sweep. Crash-resilient via `--progress` checkpoint. |
| `tools/evaluate_axis_ground_truth.py` | Per-axis bearing + length error against a candidate model output | per-pair score | Lower-level than baseline_post_218; useful for one-off candidate checks |
| `tools/evaluate_hybrid_pipeline.py` | **End-to-end production-recognizer accuracy** on the hard-case corpus (per-sticker, per-state, exact-match) | `runs/eval_results.json` + summary | Production-side eval, not global-model |
| `tools/evaluate_color_classifier_modes.py` | Color classifier accuracy under different modes (canonical / knn5_lab / knn5_lab_full ± adaptive) | per-mode confusion matrix | Used to choose `CUBE_RECOGNIZER_CLASSIFIER` default |
| `tools/evaluate_two_view_consistency.py` | A+B center-color consistency for face-ID assignment | per-pair consistency score | Two-view fusion diagnostics |
| `tools/evaluate_per_sticker_confidence.py` | Per-sticker classifier-confidence calibration | calibration plot | Color-confidence calibration |
| `tools/evaluate_mask_pipeline.py` | Mask-path end-to-end accuracy (rembg → hexagon → rectify → classify) | per-pair scores | Mask-path comparison vs current production |
| `tools/evaluate_equalize_lift.py` | Color-equalization preprocessing lift | mode comparison | Negative result — equalize doesn't close the gap |
| `tools/evaluate_geometry_labels.py` | Geometry-label round-trip via overlay JSON | per-case overlay validation | Used during May-18 overlay analysis |
| `tools/evaluate_auto_geometry.py` | Auto-geometry pipeline (silhouette → hexagon → rectify) | per-pair scores | Earlier mask-path eval |

## Specific questions → where to look

| Question | Answer in |
|---|---|
| "What is the current full-corner/facelet convention?" | `tools/FULL_CORNER_LABELING.md` + `tools/corner_conventions.py` |
| "How accurate is the current global model?" | Canonical seed answer: `tests/fixtures/full_corner_global_model_baseline.json` + `FULL_CORNER_GLOBAL_MODEL_BASELINE.md`; legacy 58-case answer: `tests/fixtures/post_218_baseline.json` + `POST_218_BASELINE_AND_TAXONOMY.md` |
| "How accurate is cv-local at producing consistent cube geometry?" | `tests/fixtures/cv_local_baseline.json` + `PHASE_1_CV_LOCAL_BASELINE.md` (90% structural fit-fail) |
| "Can existing trust signals (phase_sep + cv-local) gate retake at the Phase 2 bar?" | `tests/fixtures/phase2b_trust_signal_matrix.json` + `PHASE_2B_TRUST_SIGNAL_MATRIX.md` — **no.** Closest-to-bar rule `phase_sep_alone_T20.0` hits 66.7% recall / 30.3% GOOD FPR (bar: ≥80% / ≤10%). Phase 2A reference (T=11.7) confirmed at 45.8%/9.2%. |
| "Can recomputed global-model signals (fit_residual + ensemble_shift + hex_bezel + junction_score) close the gap?" | `tests/fixtures/phase2b_trust_signal_matrix_recomputed.json` + `PHASE_2B_TRUST_SIGNAL_MATRIX_RECOMPUTED.md` — **no, but with caveats.** No hand-tuned rule clears both bars. `phaseANDcv_OR_ensemble_shift_T60.0` is the first rule to clear FPR (8.1%) — at 50% recall. `phase_sep_alone_T20.0` is the first rule to clear recall (80%) — at 31% FPR. The two bars can be met SEPARATELY but not BY THE SAME RULE under hand-tuning. **Pivot decision is now evidence-backed.** |
| "How accurate is cv-local end-to-end on per-sticker color?" | `tools/evaluate_hybrid_pipeline.py` output (different question; production-style eval) |
| "What is current-main production solvable-rate / per-sticker accuracy on corpus + hard cases?" | `tests/fixtures/main_solvable_baseline.json` + `MAIN_SOLVABLE_BASELINE.md` |
| "Which yaw source should the hull-label path use?" | `tests/fixtures/hull_label_slot_yaw_assignment.json` + `tools/HULL_LABEL_SLOT_YAW_ASSIGNMENT.md` |
| "Is the hidden Hull-Label Tier 1 recognizer path ready to promote?" | `tests/fixtures/hull_label_tier1_recognizer_validation.json` + `tools/HULL_LABEL_TIER1_RECOGNIZER_VALIDATION.md` |
| "Where is constrained recognizer latency going?" | `tools/DEPLOYED_CONSTRAINED_RECOGNIZER_SCOREBOARD.md` + `tools/CONSTRAINED_RECOGNIZER_LATENCY_PLAN.md` |
| "Did my PR regress geometry on any case?" | `tools/baseline_post_218.py --diff prev curr` |
| "Which sets are always-failing across reruns?" | `POST_218_BASELINE_AND_TAXONOMY.md` "Case-level stability" table |
| "Why did Set X fail?" | Look up its category in the baseline JSON, then read the failure-mode meaning in `FAILURE_TAXONOMY.md`. Visual: `tools/render_global_cube_model_v0_overlays.py` for that case. |
| "Is dark-line junction extraction worth pursuing?" | No — see `tools/PATCH_JUNCTION_VERTEX_LOCALIZER_V0_REPORT.md`, `tools/RAW_PATCH_VERTEX_LOCALIZER_V0_REPORT.md`, `tools/RAY_START_VERTEX_REFINEMENT_V0_REPORT.md` (all negative results). |
| "Did SAM3 beat rembg?" | `tools/SAM3_*` and `tools/FOUNDATION_SEGMENTATION_BAKEOFF_V0_REPORT.md` — not by enough to switch. |
| "How does the chirality / phase detector work?" | `tools/NEAR_FAR_PHASE_REPORT.md`; treat legacy error-category evidence as provisional until full-corner migration |
| "Is the model choosing the correct one-edge/far parity under the human convention?" | `tests/fixtures/full_corner_global_model_baseline.json` + `FULL_CORNER_GLOBAL_MODEL_BASELINE.md` |
| "What's the full per-axis error history on a specific case?" | Run `tools/render_global_cube_model_v0_overlays.py` + check `tools/GLOBAL_CUBE_MODEL_*REPORT.md` series. |

## Cross-corpus questions

The labeled fixtures don't all overlap. Match what you're trying to
measure to the right corpus:

| Corpus | What's in it | Coverage |
|---|---|---|
| **Full-corner labels** | `Va/Vb + 0..5` corners, from which one-edge and far triplets are derived side-specifically | New canonical geometry truth. Use this to migrate legacy axis labels. |
| **Axis-labeled gallery (58)** | Vertex + `near_*` legacy triplet | Across-cube-types, includes hard cases. Seed audit says `near_*` maps to far/double-axis labels; full 58-case migration pending. |
| Easy-corpus (~30) | Per-pair production eval cases | Originally chosen as "easy" — many are now well-handled by post-#218 main |
| Hard-case corpus (~14) | Sets 12/14/17/57/58/61/62 and friends | Where production currently struggles |
| OOD corpus | Sets with non-standard cubes (Set 46 Rubik's brand, etc.) | Used to check generalization |
| 27-vertex-only corpus | Earlier vertex-only labels (superseded) | Historical |

## When to add a new benchmark

Add to this index AND to `BENCHMARK_INDEX.md` when:

- A new fixture file lands in `tests/fixtures/`.
- A new evaluator lands in `tools/evaluate_*.py` or `tools/baseline_*.py`.
- A class of question becomes recurring (e.g., "trust-policy
  abstention rate" once Phase 2 has data).

Don't add when:

- A one-shot probe answers an in-the-moment question. Keep those in
  `tools/probe_*.py` without registering them as a benchmark.
- A report is a snapshot of a single experiment (those live as
  `*_V0_REPORT.md` and don't need indexing here unless they're
  durable answers).
